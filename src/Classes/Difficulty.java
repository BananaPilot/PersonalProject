package Classes;

/**
 * Enum that lets use choose the difficulty of our exercises
 * <pre>
 *   Example:
 *   {@code
 *   exercise.setDifficulty(Difficulty.{Any of the fields below})
 *   }
 * </pre>
 */
public enum Difficulty {
  Beginner,
  Easy,
  Medium,
  Hard,
  Expert;
}