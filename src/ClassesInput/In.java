package ClassesInput;

import java.util.Scanner;

/**
 * opens a new scanner
 */
public class In {
  /**
   * opens a new scanner, so it can be used throughout all the project
   * @see java.util.Scanner
   */
  public static Scanner scanner = new Scanner(System.in);
}
